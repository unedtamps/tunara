<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Warna saat ikon dipilih (aktif) - Diubah menjadi warna baru -->
    <item android:color="@color/ocean_blue" android:state_checked="true" />
    <!-- Warna default (tidak aktif) -->
    <item android:color="@android:color/darker_gray" android:state_checked="false"/>
</selector>
